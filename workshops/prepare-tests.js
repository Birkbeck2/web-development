import { renameSubmissionsByCoder, propagateTestsToSubmissions } from '#test-utils'

export default async function prepareTests () {
  // Needs rewriting
  // await renameSubmissionsByCoder()
  await propagateTestsToSubmissions()
}
